  <div>
      <h1>Photo App</h1>
      <label>
        Enter your name:
        <input type="text" value={userName} onChange={handleInputChange} />
      </label>
      <button onClick={handleDisplayButtonClick}>Display Name on Photo</button>

      {displayedName && (
        <div>
          <h2>Hello, {displayedName}!</h2>
          {/* Render the image with the displayed name */}
          
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <img
              ref={imageRef}
              src={logo}
              alt="User Display"
              style={{ borderRadius: '50%' }}
            />
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                color: 'white',
                fontSize: '18px',
                fontWeight: 'bold',
                textAlign: 'center',
                textShadow: '2px 2px 4px #000000',
              }}
            >
              {displayedName}
            </div>
          </div>
          <button onClick={handleDownloadButtonClick}>Download Photo</button>
        </div>
      )}
    </div>

